# Skia PoC font bundle

These Noto variable fonts are copied from the nixpkgs `noto-fonts` package for the Skia/SkParagraph proof of concept.

The first PoC needs deterministic bundled fonts and does not use system font discovery or fontconfig.
